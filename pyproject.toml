[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cpnlookup"
version = "2.5.0b1"
description = "Local CLI tool for GitHub RAG, Call Graphs, and Codebase querying -- v2.0.0 - Forge"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "click>=8.0.0",
    "rich>=13.0.0",
    "requests>=2.0.0",
    "sentence-transformers>=2.2.0",
    "faiss-cpu>=1.7.0",
    "numpy>=1.21.0",
    "rank-bm25>=0.2.2",
]

[project.scripts]
lookup = "cpnlookup.cli:cli"